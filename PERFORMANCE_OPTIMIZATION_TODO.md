# EasyVtuber 性能优化审计与待办

更新日期：2026-09-02
工作分支：主项目 `codex/gpu-safety-90`；运行时子模块 `codex/trt-cache-safety`  
目标：在不降低模型精度、不改变最终图像或动作数值的前提下，提高启动速度、稳定帧时间和缓存效率，并把持续 GPU 推理占空比限制在 90% 以内。

## 1. 当前结论

项目仍有明确的无损优化空间，下一阶段值得继续。当前配置是：TensorRT、THA3 separable FP32、Medium 姿态简化、30 FPS、RAM/VRAM 缓存各 2 GB、RIFE 与超分关闭、Debug 输出、扩展移动开启、iFacialMocap 可用。

最值得优先做的四项是：

- [x] 从姿态简化与 iFacialMocap 启动路径中移除 PyTorch/旧 THA 网络的连带导入。
- [x] 为 EasyVtuber 启用严格受容量限制的无损原始帧 RAM 缓存快路径，避免每次命中都同步 Brotli 解压。
- [x] 复用后处理与共享内存输出缓冲，减少每帧临时数组和完整图像复制。
- [x] 修正 TensorRT RIFE 中间帧缓存漏写，并添加无需 GPU 的回归测试。

这四项都不更换模型、不启用 FP16/INT8、不提高姿态简化等级；验收要求是姿态逐值一致、最终帧逐字节一致。预计当前配置下，前三项可改善启动时间、CPU 占用和帧时间；RIFE 修复只有在开启插帧与 RAM 缓存时明显。

## 2. 已完成的工作与基线

- [x] 使用独立下载/分支工作，不对官方仓库执行 pull request，也不向远端推送。
- [x] 加入 24 FPS 帧率选项，并修复启动器滑块数值被截断等布局问题。
- [x] 加入持续 GPU 推理占空比上限；当前默认/保存值为 90%。
- [x] TensorRT 引擎与运行时缓存改为内容寻址，避免无意义重复构建。
- [x] TensorRT 后端延迟导入，DirectML 启动不再无条件加载 CUDA/TensorRT。
- [x] 缓存键改为精确、可处理哈希碰撞的 NumPy 元数据加字节内容。
- [x] 姿态简化量化配置缓存，减少每帧重复构造。
- [x] 无变换时跳过恒等仿射变换；Debug 模式仍复制后再画统计信息，避免污染模型/缓存帧。
- [x] 修复/增强 iFacialMocap 解析容错、临时数组复用与连续输入测试。
- [x] 对当前版本和官方最新版本进行了同设置、同输入的受控性能比较。

### 官方最新版本与当前版本对照

测试条件：`lambda_00`、TensorRT FP32 separable、Medium 简化、30 FPS、RAM/VRAM 各 2 GB、眉毛/RIFE/SR 关闭、扩展移动开启；使用 5000 帧录制的 iFacialMocap 姿态流。每个版本运行 60 秒/1800 帧，使用独立的 90% 占空比限制器。

| 指标 | 官方最新版本 | 当前版本 | 结论 |
|---|---:|---:|---|
| 核心初始化 | 12.253 s | 1.247 s | 当前快约 90%；官方值还绕过了强制引擎重建，是偏有利下限 |
| 图像预处理，中位数 | 270.467 ms | 6.955 ms | 当前显著更快 |
| `setImage` | 191.042 ms | 58.466 ms | 当前显著更快 |
| 预热 | 450.699 ms | 67.783 ms | 当前显著更快 |
| 推理平均 | 13.174 ms | 12.839 ms | 当前约快 2.5% |
| 总处理平均 | 14.205 ms | 13.726 ms | 当前约快 3.4% |
| 总处理 P95 | 24.199 ms | 23.742 ms | 当前更稳 |
| 超过 33.33 ms 的帧 | 14 | 13 | 基本相当，当前略好 |
| GPU 平均/峰值 | 30.23% / 75% | 31.30% / 63% | 当前峰值更低；平均差异在采样噪声范围 |
| 显存 | 3121 MiB | 3115 MiB | 基本相同 |
| 完整进程时间 | 76.189 s | 63.765 s | 当前少约 12.4 秒 |

精度验证：

- 30 个最终 RGBA 检查点逐字节一致。
- 5000 个简化姿态没有数值差异；仅有 240 个 `+0.0/-0.0` 符号位差异，不影响任何计算或画面。
- 因此现有优化没有发现视觉效果、透明度、颜色或动作变化。

注意：官方原版运行时在启动时会无条件重建五个 TensorRT 引擎。为了保护可能有硬件问题的显卡，比较测试绕过了这一步，所以表中的官方启动时间不是其最坏情况。

## 3. 第一阶段：低风险、无损热路径优化（已完成）

### 3.1 轻量姿态索引与 iFacialMocap 启动

现状：

- `src/utils/pose_simplify.py` 为取得固定的姿态索引，会间接导入 `tha2.poser.modes.mode_20_wx`。
- `mode_20_wx.py` 又导入 `mode_20.py`，后者会加载 PyTorch 和大量旧 THA 网络代码。
- `src/i_facial_mocap_client.py` 在模块导入时全局创建转换器，Windows 多进程父进程也会承担这段启动开销。
- 冷启动实测导入 `mode_20_wx` 约 3.77 秒；仅 NumPy 与 SciPy 优化器约 0.66 秒，大部分额外耗时来自 PyTorch/旧网络导入链。

待办与验收：

- [x] 提取固定的 45 维姿态索引到不依赖 Torch 的轻量模块。
- [x] 让 `pose_simplify` 直接使用轻量索引。
- [x] 让 iFacialMocap 转换器不再通过 `mode_20.py` 获取固定索引。
- [x] 把 IFM 转换器创建移入子进程 `run()`，避免父进程提前导入 SciPy/转换器。
- [x] 对 5000 帧 iFacialMocap 输入做逐值一致性比较。
- [x] 测量冷导入时间。

预期影响：启动更快、父进程内存更小；运行中的画面和动作必须完全不变。对 iFacialMocap 输入最明显，其他输入也会受益于姿态简化路径不再加载 Torch。

实测：`mode_20_wx` 冷导入约从 3.77 秒降到 0.32 秒；`pose_simplify` 冷导入约 0.126 秒，且不加载 Torch 或 SciPy；父进程导入 IFM 客户端约 0.163 秒，不加载 Torch、SciPy 或转换器。当前与官方旧转换器对 5000 组确定性 IFM 输入得到完全相同的 SHA-256；当前与修改前本分支对 5000 组随机姿态的简化结果为 0 个不等帧、最大绝对差 0。

### 3.2 无损 RAM 缓存快路径

现状：

- 每次缓存写入都同步 Brotli 压缩，每次命中都同步解压。
- 512×512 BGRA 代表帧实测：写入约 1.133 ms/帧，读取约 1.850 ms/帧；原始帧复制约 0.270 ms/帧。
- 当前 60 秒测试中 RAM 缓存命中 791、未命中 1012，命中率约 43.9%。压缩缓存仅占 161.1 MiB，但 CPU 压缩/解压进入了每帧热路径。
- 2 GB 原始缓存约能容纳 2048 张 512×512×4 帧，足以覆盖本次 60 秒姿态轨迹。

待办与验收：

- [x] 在保留兼容 Brotli 模式的同时，增加原始只读数组模式。
- [x] EasyVtuber 默认采用原始帧快路径；提供 `--ram_cache_mode brotli` 命令行回退。
- [x] 严格按原始/压缩后的实际字节数执行 LRU 容量限制；超大条目不得突破预算。
- [x] 缓存持有独立副本，返回数据为只读，不能被调用者意外修改。
- [x] 添加命中、淘汰、容量和逐字节一致性测试。

预期影响：当前配置命中时可明显减少 CPU 帧时间和抖动，不改变任何像素。代价是相同“2 GB”设置能缓存的帧数少于 Brotli；很长且姿态变化大的会话可能出现更多未命中。Brotli 模式保留给更重视容量的用户。

实测：同一张 512×512 角色图，Brotli 写入/命中读取中位数约为 1.138/2.038 ms；原始模式约为 0.193/0.0002 ms。100 个条目分别约占 13.81 MiB 与 100 MiB，符合“Brotli 节省容量、raw 降低延迟”的设计取舍。

### 3.3 后处理与输出缓冲复用

现状：

- 推理结果经过仿射/颜色转换后先生成新数组列表，再完整复制到共享内存。
- TensorRT 无 RIFE/SR 路径也会先 `np.copy` 一次模型输出。
- 当前 Debug + 扩展移动微基准：现路径约 0.904 ms/帧；预分配并直接写目标缓冲约 0.440 ms/帧，节省约 0.464 ms，结果逐字节一致。
- RGBA 路径约可从 0.787 ms 降到 0.425 ms。

待办与验收：

- [x] 为仿射和颜色转换复用 BGRA/RGBA 工作缓冲。
- [x] 直接写共享内存目标缓冲，取消“临时结果列表 → 再复制”的一步。
- [x] 保留 Debug 文字覆盖、VirtualCam RGB、Spout RGBA、alpha split 的现有语义。
- [x] 通过默认仍复制的 `copy_output` 可选接口，让 EasyVtuber 内部安全使用 TensorRT 可复用 host buffer。
- [x] 对所有输出模式和有/无扩展移动做逐字节回归测试。

预期影响：所有后端都可受益；当前 Debug + 扩展移动配置最明显。只减少内存分配和复制，不改变颜色、透明度、变换或调试文字。

实测：当前 512×512 Debug + 扩展移动路径，旧路径中位数约 0.847 ms，新路径约 0.464 ms，节省约 0.383 ms（约 45%）；P95 从约 1.031 ms 降到 0.649 ms。六种输出组合均逐字节等同于旧路径，源模型/缓存帧也保持不变。

### 3.4 TensorRT RIFE 缓存漏写

现状：

- TensorRT 运行时用 `range(1, len(poses) - 1)` 写入插值帧缓存。
- x2 时因此一个中间帧也不写；x3/x4 都少写一帧，与 DirectML 和调用方姿态列表语义不一致。

待办与验收：

- [x] 改为缓存全部 `poses[:-1]` 对应的 RIFE 中间结果。
- [x] 添加 x2/x3/x4 的纯 mock 测试，确认键与帧对应关系。
- [x] 当所有中间帧命中且没有后续 SR 时，跳过不必要的 Host-to-Device 上传与空同步。

预期影响：仅在“开启 RIFE 插帧 + 开启 RAM 缓存”时明显，可提高后续帧命中率并减少 GPU 工作；输出像素不变。当前 RIFE 关闭，因此不会改变当前配置的性能或画面。

全缓存路径现在复用第一次查询到的 RIFE 帧：无 TensorRT SR 时，x2/x3/x4 每批分别避免约 3/4/5 MiB 的 Host-to-Device 上传（当前 THA 帧 1 MiB + 完整 RIFE 批次 2/3/4 MiB），并删除主流及 THA 缓存流的空同步。若后续是 TensorRT SR，仍上传 SR 真正需要的完整批次，但不再上传从未被消费的当前 THA 设备帧，也不在同一 CUDA 流的连续操作之间额外同步；Anime4K 是 CPU/OpenCL 路径，同样无需 CUDA 上传。x2/x3/x4 mock 均确认返回帧逐值一致且上传/同步调用为零。当前没有已缓存的 RIFE TensorRT 引擎，为避免仅为验证一个无 GPU 的全缓存分支而触发高负载构建，本轮没有新建 RIFE 引擎。

## 4. 第二阶段：TensorRT 资源管理与启动安全

### 4.1 VRAM 缓存内存池

- [x] 为 `vram_cache.py` 增加按字节数精确分类的空闲缓冲池；淘汰条目优先复用同尺寸分配，尺寸不匹配时先释放空闲块再分配，活动缓存加空闲池的保留显存始终不超过用户预算，并记录 allocation/reuse/eviction/pool-release 计数。
- [x] 完成 CUDA 隔离、像素一致性和 1800 帧受控长测，统计分配/复用/淘汰、P95/P99、预算和峰值显存；填满后实际分配次数保持不变，没有发现持续分配或碎片增长迹象。

修改前长测中 VRAM 命中 499、未命中 513，约 409 个条目占满 2 GB 后发生约 104 次淘汰。该项只影响 TensorRT + VRAM 缓存，理论上不改像素，但涉及 CUDA 内存生命周期，因此已在 CPU/mock 验证后执行受控 GPU 测试。

内存池验收结果：

- 8 MiB CUDA 隔离测试连续写入 100 个 1 MiB 块：实际分配 8 次、复用/淘汰各 92 次，最终块完整回读，清空后保留字节归零。
- 50 MiB 极端淘汰测试：160 帧中 308 次淘汰，仅实际分配 30 次并复用 614 次；与完全关闭 VRAM 缓存的同输入最终 SHA-256 均为 `e312f229d3f10866cc32d2055fa3f69b07a9a37e1208b56a16f2568123603124`，像素逐字节一致。
- 2 GiB、1800 帧、600 个唯一姿态循环三次的 90% 持续占空比长测：55.848 秒，平均/中位/P95/P99/最大分别为 16.360/16.320/16.628/17.043/28.891 ms。为持续制造淘汰，本测试的姿态复用距离刻意大于缓存容量，因此命中为 0。
- 长测在第 600 帧达到 1,224 次实际分配后直到第 1800 帧不再增加，最终复用 5,980 次、淘汰 2,990 次、pool release 0；612 个活动条目保留 2,145,779,712 字节，低于 2 GiB 预算 2,147,483,648 字节。
- CUDA 驱动采样的峰值总显存占用约 4,221,042,688 字节；测试前后温度 47°C/70°C，进程退出后回落到 331 MiB、利用率 0%。`nvidia-smi` 只在前后各同步执行一次，最终残留进程为 0。五个引擎和五个运行时缓存均在测试前精确确认存在，未发生构建。

### 4.2 持久化 TensorRT 缓存与构建保护

- [x] 启动器加入安全的手动清理入口：只删除已识别的 TensorRT 引擎/运行时磁盘缓存，不递归删除目录；启动器管理的 EasyVtuber 正在运行或存在构建锁时拒绝清理，并明确警告下次启动会重建引擎、可能短时高 GPU 负载。RAM/VRAM 缓存随进程退出自动释放，不纳入清理。
- [x] 默认引擎缓存从 `%TEMP%` 移到 `%LOCALAPPDATA%\EasyVtuber\trt-cache`，保留环境变量覆盖；首次使用时原子迁移已有的有效引擎/运行时缓存，跳过残缺临时文件和无关文件，存在构建锁时拒绝迁移，避免路径切换触发意外重建。
- [x] TensorRT 每次启动前显示持久化缓存位置、引擎数量和容量并要求明确确认，默认选择“否”；缓存为零时明确提示本次必须构建，存在候选缓存时仍警告所选模型/精度/GPU/版本不匹配会触发重建，并说明 90% 持续占空比无法硬性限制单次构建的瞬时峰值。缓存状态检查不导入 CUDA/TensorRT。
- [x] 构建锁结合锁内 PID 判断：进程已退出时立即回收，无需先等 600 秒；PID 仍存活时即使超过 900 秒也不删除；PID 缺失/损坏时才使用 900 秒年龄兜底。启动检查、缓存迁移和清理共用同一安全判定。
- [x] 使用缓存目录内原子更新的 `hash-manifest-v1.json` 跨进程复用 ONNX SHA-256；仅在绝对路径、大小、mtime 和 ctime 全部一致时命中，任一状态变化或清单损坏都会回退完整哈希，清理缓存时一并删除清单。

清单微基准：当前 THA3 separable FP32 的五个 ONNX 共 224,788,839 字节；首次完整 SHA-256 约 0.211 秒，重新加载模块后从持久化清单取值约 0.0033 秒（文件已在系统缓存中，约快 64 倍），五个条目均命中。

引擎构建本身是不可细分调用，构建后的冷却无法阻止调用期间瞬时 100% 利用率。因此最有效的硬件保护是让已构建引擎可靠持久化，并在缺失时先提示。任何 GPU 测试都继续遵守 90% 持续占空比，但瞬时利用率仍可能高于 90%。

### 4.3 TensorRT 小型开销清理

- [x] 正常执行时不再创建或记录未被使用的 CUDA start/end event；仅当构造参数 `profile_inference=True` 或环境变量 `EZVTB_TRT_PROFILE=1` 时启用计时，禁用状态下读取计时会给出明确错误。
- [x] 验证并删除 `CoreTRT` 中从未提交任务的独立 `cache_stream` 及每帧空同步；实际 VRAM 缓存流仍是 `self.tha.cachestream`，90% 安全模式的收尾同步保留。
- [x] 保持引擎/上下文串行加载：当前 THA、RIFE、SR 构造顺序逐个完成，上游并行方案曾回退，并行还会增加启动峰值 GPU 与显存压力，因此明确不采用。

验收结果：新增 CPU/mock 测试确认默认创建 0 个计时 event、enqueue 记录 0 次，显式参数与环境变量两种 opt-in 均可恢复计时；运行时完整测试 44 项通过。实机用五个已缓存的 THA3 separable FP32 引擎运行 25 个姿态两轮，五个引擎均确认计时关闭，第二轮 25 次全部命中 RAM 缓存；两轮输出 SHA-256 均为 `4df544b1cd18c63a21317ef3b000d116b33d8f2de386cc119738b61cc0d3ced2`。初始化 1.163 秒，首轮/命中轮平均推理分别为 16.783/0.392 ms；未发生引擎构建，也未调用或遗留 `nvidia-smi`。预期收益较小，不影响精度。

### 4.4 学生模型与超分缓存预算

- [x] `CoreTRT` 将用户的 `vram_cache_size` 原样传给 `THA4StudentEngines`，不再静默退回默认 1 GB；mock 测试同时确认自定义模型从 `init_model_path()` 配置的统一模型根目录解析。
- [x] THA4 student 包统一要求 `face_morpher.onnx`、`body_morpher.onnx` 和 `character.png`：启动器不再寻找运行时无法直接使用的 `.trt`，TensorRT 按内容寻址生成磁盘引擎缓存，DirectML 直接加载同一 ONNX。主程序现在真正读取所选 student 包内的角色图，并拒绝越出模型根目录的名称。
- [x] RAM 缓存设置改为普通帧和 SR 帧的单一总预算；开启 SR 时按 512²:1024² 的每帧字节量 1:4 拆分，设置 2 GB 即普通帧 0.4 GB、SR 帧 1.6 GB，总和不超过 2 GB。TensorRT、DirectML 和 Anime4K 路径使用同一规则；SR 关闭时普通帧仍获得完整预算。

验收结果：新增 student 包扫描/角色路径/目录穿越、VRAM 参数传递、缓存预算拆分测试；当前主项目 38 项、运行时 49 项 CPU/mock 测试全部通过。当前配置关闭 THA4 student 与 SR，因此性能、缓存容量和画面均无变化。开启 SR 后，实际 RAM 上限会从旧版意外的最多两倍设置值降到用户选择值，代价是相对于旧版越界行为可能更早淘汰缓存；只影响命中率，不影响精度或像素。

## 5. 第三阶段：高收益但高复杂度路径

### 5.1 DirectML 全 GPU 驻留管线

- [ ] 把 `CoreORT` 的 RIFE/SR 从普通 `.run()` 和 NumPy 往返改为 OrtValue/I/O Binding。
- [ ] 尝试让 THA → RIFE → SR 中间张量留在 GPU，只把最终图像读回 CPU。
- [x] 删除 `tha3_ort.py`、`tha4_ort.py` 中未使用的 `onnx` 导入；TensorRT/DirectML 均只在实际启用 Anime4K 时导入 `pyanime4k`。

本机 ONNX Runtime 1.23.0 原型已证明 GPU 驻留在接口和精度上可行：RIFE x2 FP32 的 DML OrtValue 输出可直接绑定为 waifu2x x2 FP32 输入，最终 `(2, 1024, 1024, 4)` `uint8` 图像与普通 `.run()` 管线逐字节一致。但五轮稳态均值仅从 1028.745 ms 降到 1026.047 ms，约快 0.3%/2.7 ms；首次运行时间受 DML JIT 顺序影响，不计入结论。鉴于生产实现还必须覆盖动态批次、RAM 缓存全命中/部分命中、device 0 限制和各条回退路径，当前收益不足以承担全面改写风险，因此前两项保留为未来针对更快 DirectML 设备重新评估的待办。

低风险导入清理实测使 `core_ort` 冷导入约从 0.471 秒降到 0.388 秒，且导入后 `onnx`、`pyanime4k` 均不在已加载模块中；当前运行时 49 项 CPU/mock 回归通过。这部分不改变模型或像素；Anime4K 首次真正启用时才支付其约 0.034 秒模块导入成本。当前 TensorRT + 非 Anime4K 配置也获得这项小幅启动收益。

### 5.2 Spout 输出路径

- [x] 验证当前 `PySpout.pyd` 不能原生接受 BGRA，因此保留 CPU BGRA→RGBA 转换以避免红蓝通道互换；构造器只把 `GL_RGBA` 识别为四通道，传 `GL_BGRA` 会被错误按三通道处理。
- [x] 为现有 `int32` 绑定复用一块连续 staging buffer，避免 pybind11 每帧隐式分配并强制转换整张 `uint8` 图像。
- [ ] 若继续优化 Spout，先重编译/替换 Python 绑定：当前 `send_image` 接口要求 `int32` 数组，再由 C++ 逐元素取低 8 位复制到内部 `uint8` 缓冲；应改为校验并直接读取连续 `uint8` RGBA，随后做 OBS 逐像素与吞吐验证。
- [ ] 长期研究 CUDA/OpenGL/D3D 纹理互操作，减少最终 GPU→CPU→GPU 往返。

CPU 微基准显示，现有复用目标缓冲的 BGRA→RGBA 转换在 512/1024 分辨率中位数仅约 0.068/0.132 ms；绑定隐含的分配式 `uint8→int32` 转换约为 0.815/3.063 ms。改用复用 staging buffer 后降至约 0.250/0.948 ms，约 3.2 倍，并以 4/16 MiB 常驻缓冲替代逐帧同尺寸临时分配；逐值测试覆盖 0、255 及多组 RGBA 通道且完全一致。C++ 内部逐元素截断和最终发送仍存在，所以重编译原生 `uint8` 绑定仍是下一步。该方向只在 Spout 输出时明显，1024 超分收益更大；当前 Debug/VirtualCam 输出无影响。尚未连接 OBS 做实时接收吞吐验证。

### 5.3 跨引擎 CUDA Graph / 模型融合

- [x] 研究跨 THA/RIFE/SR 的 CUDA Graph 或融合 ONNX。
- [x] 权衡启动/显存、缓存跳过能力和浮点末位变化。

当前每个 TensorRT 引擎已各自使用 whole-graph capture；本机 TensorRT RTX 1.3 Python API 只暴露单个 execution context 的 `WHOLE_GRAPH_CAPTURE`，没有跨 context 的公开捕获入口。融合 ONNX 或自行维护多套跨引擎图虽然可能减少完整未命中时的 kernel launch，但 THA3/THA4 当前会根据 morpher/combiner 缓存命中动态跳过不同子管线，RIFE/SR 也有独立缓存分支；固定融合图会丢失这些跳过收益，并增加启动时间、显存和图变体维护成本。融合还可能改变算子划分与浮点计算顺序，无法满足“输出精度不变”的严格条件。因此该项审计完成，当前不实施；若未来用固定姿态序列、关闭中间缓存做专用基准，再单独重新评估。

## 6. 低优先级整理

- [x] 用 Windows 高精度 waitable timer 评估替换每帧最后约 0.5 ms 的忙等。
- [x] 评估无锁单写者共享统计值，减少 `multiprocessing.Value` 每帧锁开销。
- [x] 修正 `FPS.view` 的显示计算偏差；它不影响真实运行性能。
- [x] 替换启动器中缓慢且已弃用的 WMIC NVIDIA 检测。
- [x] 将 PyTorch 降为旧训练/开发工具的可选依赖（需先完成轻量姿态索引）。
- [x] 固定关键依赖版本，提高老项目的可复现性。
- [x] 审计不再进入运行路径的旧 THA2/THA3/THA4 训练代码；因体积很小且仍有开发/IFM 用途，保留源码。
- [x] 评估单进程/线程重构；当前返回帧本已是共享内存零复制视图，同步收益不足，暂不重构。

Windows 运行路径现在优先使用可复用的高精度 waitable timer，并在不支持时回退到原有 sleep/spin；0.5 ms 定时分辨率请求在进程退出时会明确释放。2000 次、每次仅等待 0.3 ms 的短等待基准中，旧忙等消耗约 0.719 秒 CPU，新实现约 0.063 秒，代价是中位过点从约 0.0001 ms 增至 0.199 ms；2000 次 3 ms 等待中，中位过点由约 0.454 ms 降至 0.112 ms，P95 约为 0.510/0.554 ms。它不改变图像字节，只可能让极短的补齐等待晚约 0.2 ms，且不参与 GPU 90% 占空比限制器。

单写者共享统计值微基准中，加锁/无锁的一写一读约为 1.021/0.140 微秒；实际每帧只有少量统计访问，总收益不足 0.01 ms，而 `RawValue` 会放弃跨进程同步保证，因此保留现有锁。`FPS.view` 已改为用 N 个时间戳对应的 N−1 个区间计算，只修正状态栏、控制台及 Debug 叠字数值。启动器 NVIDIA 检测已由 WMIC 子进程替换为 Win32 `EnumDisplayDevicesW`，本机能直接识别 Intel 核显与 RTX 4060，启动器导入约 0.08 秒且不再出现 WMIC 拒绝访问。

PyTorch 已从基础 `requirements.txt` 移到 `requirements-dev.txt`，仅供未进入正式入口的旧 PyTorch 模型、训练和 ONNX 导出脚本使用。当前嵌入式环境仍保留已安装的 PyTorch，不做破坏性卸载；未来重新打包时可至少少装约 433 MiB 的 PyTorch 本体。实测单独导入当前 CPU 版 PyTorch 约需 2.29 秒，但启动器、TensorRT/DirectML 运行路径和 iFacialMocap 均不导入它；新增测试会主动阻止任何 `torch` 导入并验证这些路径。

基础运行依赖已固定为当前完整回归通过的精确版本；发布脚本也固定 Python 3.10.19、PyCUDA 2025.1.2、CUDA 12.9.1/NVCC 12.9.86 和 cuDNN 9.17.0.29。原依赖同时要求 `opencv-python`，而 MediaPipe 又会安装拥有同一 `cv2` 文件树的 `opencv-contrib-python`；现在只要求本机已验证的 contrib 4.13.0.90，避免安装顺序静默覆盖二进制。版本锁测试会确认每个直接运行依赖均与已验证环境一致，`pip check` 无冲突。该改动不会更换当前环境里的任何包，只影响未来安装/重新打包。

THA2/THA3/THA4 旧源码全部文件合计约 1.18 MiB，其中正式入口仍需要 THA2 的 IFM 常量、姿态索引和 SciPy 转换器；删除或复制出最小子集只会增加维护分叉，还会破坏保留的旧开发/导出工具，因此不为这点体积删除源码。输出帧则已由模型进程直接写入共享内存，主进程读取同一 NumPy 视图，不存在额外整帧 IPC 复制。无竞争命名互斥 acquire+release 约 1.25 微秒，Event set+wait+clear 约 7.03 微秒；即 x4 插帧的基础同步成本也约 0.02 ms/批。单进程重构会失去输入、推理和输出阻塞隔离，当前不值得实施。

## 7. 视觉与动作影响总表

| 优化方向 | 当前配置是否明显 | 预期视觉/动作影响 |
|---|---|---|
| 轻量姿态/IFM 导入 | 是，尤其 IFM 启动 | 无；要求 5000 帧逐值一致 |
| 原始帧 RAM 缓存 | 是，缓存命中时 | 无；逐字节一致 |
| 后处理缓冲复用 | 是，Debug + 扩展移动更明显 | 无；逐字节一致 |
| RIFE 缓存与全命中主机快路径 | 否，当前关闭 RIFE | 无；开启插帧后提高命中率并避免无用 GPU 上传 |
| VRAM 内存池 | 长时间 TensorRT 会话 | 无；只改内存复用 |
| TensorRT 事件/同步清理 | 小幅 | 无 |
| THA4 student 包与 VRAM 预算 | 否，当前使用 THA3 | 无；修正模型发现、角色图和缓存设置传递 |
| SR 缓存预算 | 否，当前关闭 SR | 无；严格限制总内存，可能降低相对旧版越界容量下的命中率 |
| DirectML I/O Binding | 否，当前用 TensorRT | 理论无；仍需驱动相关像素验证 |
| Spout 复用转换缓冲/未来原生输出 | 否，当前 Debug | 复用缓冲逐值一致；未来原生绑定仍需 OBS 验证通道和 alpha |
| Windows 帧等待/FPS/显卡检测 | 所有配置均经过，短补齐等待更明显 | 像素无变化；极短等待可能晚约 0.2 ms，FPS 显示更准确 |
| PyTorch 可选依赖 | 未来重新安装/打包时明显 | 无；正式运行路径不使用 PyTorch |
| 依赖版本锁与单一 OpenCV 包 | 未来重新安装/打包时明显 | 无；固定为当前已验证二进制组合 |
| 保留多进程与旧开发源码 | 所有配置 | 无；审计后维持现状，避免高风险低收益重构 |
| FP16/INT8/更高简化等级 | 不在本计划内 | 可能改变精度或动作，明确不采用 |

### 3.5 第一阶段集成验证结果

- 主项目完整 CPU 测试：38 项通过。
- `ezvtuber-rt` CPU/mock 测试：49 项通过（包含磁盘清理、持久化迁移、PID 构建锁、哈希清单、VRAM 内存池、TensorRT 计时 opt-in、student 配置、RAM 总预算及 RIFE 全缓存主机快路径测试）。
- 实机测试前确认 THA3 separable FP32 的五个内容寻址引擎全部存在，运行日志显示五个引擎和运行时缓存均直接加载，没有构建。
- 短时 TensorRT 测试：300 帧、100 个唯一姿态循环三次、90% 持续推理占空比目标、raw RAM 缓存、Debug + 扩展移动后处理。
- 实机结果：初始化 2.292 秒；300 帧循环 3.208 秒；推理平均 5.328 ms、中位数 0.048 ms、P95 16.259 ms、最大 24.558 ms。中位数很低是因为后 200 帧主要命中 RAM 缓存，不代表未命中模型推理只需 0.048 ms。
- RAM 缓存：202 次命中、101 次未命中、101 个条目、101 MiB；201 个循环结果来自只读 raw 缓存。
- 默认复制输出与 EasyVtuber 内部零复制输出逐字节一致；300 张最终 Debug 帧完成稳定哈希。
- 测试前后 GPU 温度分别为 49°C/48°C；进程结束后显存回落到约 257 MiB。`nvidia-smi` 仅执行前后各一次的单次查询，最终确认残留进程为 0。

## 8. 每阶段统一验收与安全规则

- [x] 主项目 CPU 单元测试全部通过（当前：38 项）。
- [x] `ezvtuber-rt` 无 GPU mock 单元测试全部通过（当前：49 项）。
- [x] 使用 5000 组输入验证 IFM 转换及修改前后的姿态简化；数值差异为零。
- [x] 使用固定输入/姿态检查最终帧；无损路径逐字节一致。
- [x] 只在 CPU、mock 和一致性测试通过后运行有界 GPU 测试。
- [x] GPU 测试保持 90% 持续推理占空比目标，并记录温度、显存和帧时间。
- [x] `nvidia-smi` 只执行一次性、有界命令；测试结束后确认没有遗留 `nvidia-smi` 进程。
- [x] 不执行 push、不创建 pull request；阶段完成后仅提交到本地分支。

## 9. 当前执行顺序

1. [x] 轻量姿态索引与 iFacialMocap 延迟初始化。
2. [x] 原始帧 RAM 缓存快路径及容量测试。
3. [x] 后处理缓冲复用与所有输出模式逐字节测试。
4. [x] TensorRT RIFE 缓存漏写修复与 mock 测试。
5. [x] 完整 CPU/mock/iFacialMocap 一致性回归。
6. [x] 短时、有界、90% 占空比的 GPU 验证。
7. [x] 回填本文件的实测结果并在两个本地分支提交。
8. [x] TensorRT 持久化缓存、构建确认、PID 锁和 ONNX 哈希清单。
9. [x] VRAM 精确尺寸内存池及 1800 帧受控长测。
10. [x] 默认关闭 TensorRT 计时 event，并删除每帧空流同步。
11. [x] THA4 student 配置/打包一致性与 SR RAM 总预算修正。
12. [x] DirectML 导入清理与 RIFE→SR GPU 驻留可行性原型；生产重写因本机稳态收益仅约 0.3% 暂缓。
13. [x] 审计 Spout 像素格式与绑定复制；确认必须保留 RGBA 转换，后续收益点是重编译 `uint8` 绑定或纹理互操作。
14. [x] 审计跨引擎 CUDA Graph/模型融合；现有 API、动态缓存分支和逐字节一致性约束下暂不实施。
15. [x] Windows waitable timer、FPS 统计和无 WMIC 显卡检测；无锁共享统计因收益不足而不采用。
16. [x] 将 PyTorch 移到可选开发依赖，并验证启动器、DirectML 与 iFacialMocap 在禁止导入 torch 时仍可加载。
17. [x] 固定已验证的 Python/CUDA/PyCUDA/cuDNN 与直接运行依赖版本，并消除双 OpenCV 发行包要求。
18. [x] 审计旧 THA 源码与多进程 IPC；源码仅约 1.18 MiB、同步约 0.02 ms/批，均保留现状。
19. [x] TensorRT RIFE 全缓存主机快路径；无 SR 时跳过全部无用 H→D 上传与空同步。
20. [x] Spout 复用 `int32` staging buffer；512/1024 转换约提速 3.2 倍，通道值保持一致。
